#!/usr/bin/env bash
# meanwhile launcher. Runs the viewer with the system Python 3.
# Pass-through flags: --interval <s>, --refresh, --refresh-only
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$DIR/news.py" "$@"
